<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_keyboard"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_keyboard"
        android:supportsRtl="true"
        android:theme="@style/Theme.PKMobileKeyboard">

        <!-- Activity Pengaturan & Pengelola Shortcut Auto-text -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Service Utama Input Method Editor (IME) -->
        <service
            android:name=".service.CustomKeyboardService"
            android:label="@string/keyboard_service_name"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:exported="true">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

    </application>

</manifest>
